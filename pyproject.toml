[project]
name = "Betrax"
version = "0.1.0"
description = "Default template for PDM package"
authors = [
    {name = "clau", email = "clau@app2one.com"},
]
dependencies = ["Django==6.0.*"]
requires-python = ">=3.14"
readme = "README.md"
license = {text = "MIT"}

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"


[tool.pdm]
distribution = true
